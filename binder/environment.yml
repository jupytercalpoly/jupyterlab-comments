name: jupyterlab-comments-demo

channels:
  - conda-forg

dependencies:
  # runtime
  - python >=3.8,<3.9.0a0
  - jupyterlab >=3.1.0b1,<4.0.0a0
  # labextension build
  - nodejs >=15,<16
  - pip
  - wheel
  # extensions
  - jupyterlab-link-share